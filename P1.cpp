#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double volume,d,h;
    // Place your solution code for calculating the area of the triangle using the Heron's formula here
    
    
    // Use this code to output the volume of the cylinder
    cout.setf(ios::fixed);
    cout.precision(2);
    cout << "\nThe volume of the cylinder is " << volume << endl;

    return 0;
}
